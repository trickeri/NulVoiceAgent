{
  "_comment": "Optional. Codex Stop hook -> spoken completion announcement. Put this at .codex/hooks.json in a project (or your global Codex hooks config). `nulvoiceagent` must be on PATH. codex-done forks the speaking work and returns {} immediately so it never blocks the finished turn.",
  "hooks": {
    "Stop": [
      { "command": ["nulvoiceagent", "codex-done"] }
    ]
  }
}
